name: CI

on:
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python-version: ["3.9", "3.10", "3.11", "3.12", "3.13"]
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Install uv and Python
      uses: astral-sh/setup-uv@v5
      with:
        python-version: ${{ matrix.python-version }}
    
    - name: Set up Rust
      uses: actions-rust-lang/setup-rust-toolchain@v1
    
    - name: Set PyO3 environment
      run: |
        PYTHON_PATH=$(uv run which python)
        echo "PYO3_PYTHON=${PYTHON_PATH}" >> $GITHUB_ENV
        echo "LD_LIBRARY_PATH=$(dirname ${PYTHON_PATH})/../lib" >> $GITHUB_ENV
    
    - name: Install just
      uses: extractions/setup-just@v2
    
    - name: Install dependencies
      run: just install
    
    - name: Build project
      run: just build
    
    - name: Run tests
      run: just test

  format-and-lint:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Install uv and Python
      uses: astral-sh/setup-uv@v5
      with:
        python-version: "3.13"
    
    - name: Set up Rust
      uses: actions-rust-lang/setup-rust-toolchain@v1
    
    - name: Set PyO3 environment
      run: |
        PYTHON_PATH=$(uv run which python)
        echo "PYO3_PYTHON=${PYTHON_PATH}" >> $GITHUB_ENV
        echo "LD_LIBRARY_PATH=$(dirname ${PYTHON_PATH})/../lib" >> $GITHUB_ENV
    
    - name: Install just
      uses: extractions/setup-just@v2
    
    - name: Install dependencies
      run: just install
    
    - name: Format and lint
      run: just format